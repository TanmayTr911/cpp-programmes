->The mode(%) operator cannot be used for float
->In c++ if float value is given instead of int the compilor truncate the value to int,For eg: 34.56 -> 34.
-> a incomplete code to find number of prime devisor of a given number
         #include<iostream>
using namespace std;

int main(){
    int num;
    int count;
    cout<<"enter a number whose prime factors are required";
    cin>>num;
    int r;
    for(r=2;r<=num;r++){
        for(int n=2;n<r;n++){
            if(r%n!=0){
                for(count=0;r%n==0;count++){
                    num=num/r;
                    }
            }       
            else{

                }

            
        
        }   
    cout<<"number of factors "<<r<<"are"<<count;
    }
}

-> 

